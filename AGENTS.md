# 实验工程规范

本规范适用于当前实验仓库。当前目录是独立 Git 仓库，并由科研总库作为 submodule 引用。

## 权限

- 写入仅限当前实验仓库，父级科研总库和其他实验默认只读。
- 未经明确要求，不得修改父仓库、`.gitmodules` 或 submodule 指针。
- 保留用户已有修改，不得擅自回滚、覆盖或改写历史。

## 架构

实验必须包含：

```text
AGENTS.md README.md CHANGELOG.md
archive/ docs/ tmp/ trash/ tool/
data/ design/ report/ out/ src/ tests/
```

不得随意省略、改名或创建同义目录。

## 工作边界

- 正式实现放入 `src/`，正式测试放入 `tests/`。
- 实验方案和配置放入 `design/`，不创建 `configs/`。
- 除非另有说明，所有实验均在 `tmp/` 中运行。
- 辅助工具放入 `tool/`，一次性内容放入 `tmp/`。
- 原始运行结果放入新的 `out/<时间>-<实验名>/`，禁止覆盖。
- 整理后的分析和结论放入 `report/`。
- 大型或可再生数据不纳入 Git，但必须保留数据说明或获取方法。
- 重要的设计、数据、行为和复现方式变化应更新 `CHANGELOG.md`。

## Git

- 修改前确认当前 Git 根目录和工作区状态。
- 不提交 `tmp/`、`trash/`、原始输出、缓存、密钥或大型可再生数据。
- 每次提交只处理一个清晰目的。
- 不得自动更新父仓库的 submodule 指针。

完成任务时报告测试、输出位置、Git 状态和提交标识（如有）。
